simple_scouting.html
    Add PWA installation
    Add button to submit form to backend PHP file (maybe php?), which saves it to some kind of database (sql?)
    Add button to clear form
    Add year selector - shows different form
    Add onbeforeunload event to confirm page exit (only triggers if any form value isn't at its default)
